# include <stdio.h>
# include <stdlib.h>

int main(){

	int i, n, *list = NULL;
	scanf("%d", &n);
	list = (int) calloc(n, sizeof(int));
	for (i = 0; i < n; i++){
		scanf("%d", &list[i]);
	}
	// sort
	//
	//
	
	printf("");
	return 0;
}
