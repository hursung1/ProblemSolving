# include <iostream>

using namespace std;

int main(){
	int min, max, ind = 0, **num = NULL;
	bool isPrime;
	cin >> min >> max;
	
	num = 

	/*
	int num[max-1] = {0}, prime[max-min+1] = {0};	
	
	for(int i = 0; i < max - 1; i += 1){
		num[i] = i + 2;
		if(i < (max-min+1)){
			prime[i] = i + min;
		}
	}

	for(int i = 0; num[i]*num[i] <= max; i += 1){
		for(int j = 0; j < max-min+1; j += 1){
			if((prime[j] != 0) && (prime[j] != num[i])){
				if(prime[j] % num[i] == 0)
					prime[j] = 0;
			}
		}
	}

	
	for(int i = 0; i < max - 1; i += 1){
		if(num[i] != 0){
			for(int j = i; j < max - 1; j += 1){
				if(prime[j] != 0){
					if(prime[j] % prime[i] == 0){
						prime[j] = 0;
					}
				}
			}
		}
	}

	for(int i = 0; i < max - min + 1; i += 1){
		if(prime[i] != 0){
			cout << prime[i] << endl;
		}
	}*/

	return 0;
}
